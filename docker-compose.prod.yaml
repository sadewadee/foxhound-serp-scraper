services:
  db:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=serp
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB:-serp_scraper}
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U serp"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --requirepass "${REDIS_PASSWORD}" --save 60 1 --maxmemory 512mb --maxmemory-policy noeviction
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  manager:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - POSTGRES_DSN=postgres://serp:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-serp_scraper}?sslmode=disable
      - REDIS_ADDR=redis:6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - API_SECRET=${API_SECRET}
      - API_ADMIN_PASSWORD=${API_ADMIN_PASSWORD}
      - API_KEY=${API_KEY}
      - MORDIBOUNCER_SECRET=${MORDIBOUNCER_SECRET}
      - DOKPLOY_URL=${DOKPLOY_URL}
      - DOKPLOY_API_KEY=${DOKPLOY_API_KEY}
      - TZ=Asia/Jakarta
    ports:
      - "8080:8080"
      - "9090:9090"
    command: ["run", "-stage", "none"]
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2g

  seed:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - POSTGRES_DSN=postgres://serp:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-serp_scraper}?sslmode=disable
      - REDIS_ADDR=redis:6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - MORDIBOUNCER_SECRET=${MORDIBOUNCER_SECRET}
      - TZ=Asia/Jakarta
    command: ["run", "-stage", "serp", "-workers", "2"]
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2g

  enrich-1:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - POSTGRES_DSN=postgres://serp:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-serp_scraper}?sslmode=disable
      - REDIS_ADDR=redis:6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - MORDIBOUNCER_SECRET=${MORDIBOUNCER_SECRET}
      - TZ=Asia/Jakarta
    command: ["run", "-stage", "enrich", "-workers", "20"]
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2g

  enrich-2:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - POSTGRES_DSN=postgres://serp:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-serp_scraper}?sslmode=disable
      - REDIS_ADDR=redis:6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - MORDIBOUNCER_SECRET=${MORDIBOUNCER_SECRET}
      - TZ=Asia/Jakarta
    command: ["run", "-stage", "enrich", "-workers", "20"]
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2g

volumes:
  pg_data:
  redis_data:
